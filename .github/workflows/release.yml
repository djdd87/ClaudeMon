name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          dotnet-quality: 'preview'

      - name: Extract version from tag
        id: version
        shell: pwsh
        run: |
          $version = "${{ github.ref_name }}".TrimStart('v')
          echo "version=$version" >> $env:GITHUB_OUTPUT
          Write-Host "Version: $version"

      - name: Restore
        run: dotnet restore ClaudeMon.slnx

      - name: Test
        run: dotnet test ClaudeMon.slnx -c Release

      - name: Publish
        run: >
          dotnet publish src/ClaudeMon/ClaudeMon.csproj
          -c Release
          -r win-x64
          --self-contained
          -p:PublishSingleFile=true
          -p:Version=${{ steps.version.outputs.version }}
          -o ./publish

      - name: Install WiX
        run: dotnet tool install --global wix

      - name: Build MSI
        run: >
          wix build installer/ClaudeMon.wxs
          -d PublishDir=publish
          -d Version=${{ steps.version.outputs.version }}
          -o ./output/ClaudeMon-${{ steps.version.outputs.version }}.msi

      - name: Create portable zip
        shell: pwsh
        run: Compress-Archive -Path ./publish/* -DestinationPath ./output/ClaudeMon-${{ steps.version.outputs.version }}-portable.zip

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            ./output/ClaudeMon-${{ steps.version.outputs.version }}.msi
            ./output/ClaudeMon-${{ steps.version.outputs.version }}-portable.zip
